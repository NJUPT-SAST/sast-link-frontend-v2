# Shell scripts run in WSL/Linux and must keep LF. With core.autocrlf=true a
# Windows checkout would otherwise rewrite them to CRLF, and `set -euo pipefail`
# then fails as `pipefail\r: invalid option name`.
*.sh text eol=lf
